import {familyStatuses, familyTypes, familyTypes2} from '../Family'

const family = {
    familyStatuses,
    familyTypes,
    familyTypes2
}

export const familyReducer = (state = family) => {
    return state
}